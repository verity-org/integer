name: python
description: "Python runtime — minimal Wolfi-based image, zero CVE guarantee"
upstream:
  package: "python-{{version}}"

types:
  default:
    base: wolfi-base
    packages: ["python-{{version}}", "libstdc++"]
    entrypoint: /usr/bin/python3
    work-dir: /app
    paths:
      - {path: /app, type: directory, uid: 65532, gid: 65532, permissions: 0o755}

  dev:
    base: wolfi-dev
    packages: ["python-{{version}}", "py3-pip", "python-{{version}}-dev", "libstdc++", "build-base", "git"]
    entrypoint: /usr/bin/python3
    work-dir: /app
    paths:
      - {path: /app, type: directory, uid: 0, gid: 0, permissions: 0o755}

  fips:
    base: wolfi-fips
    packages: ["python-{{version}}", "libstdc++"]
    entrypoint: /usr/bin/python3
    work-dir: /app
    environment:
      OPENSSL_MODULES: /usr/lib/ossl-modules
      OPENSSL_CONF: /etc/ssl/openssl-fips.cnf
    paths:
      - {path: /app, type: directory, uid: 65532, gid: 65532, permissions: 0o755}

versions:
  "3.10":
    eol: "2026-10-31"
  "3.11":
    eol: "2027-10-31"
  "3.12":
    eol: "2028-10-31"
  "3.13":
    eol: "2029-10-31"
  "3.14":
    eol: "2030-10-31"
    latest: true
