include: ../../../_base/wolfi-dev.yaml

contents:
  packages:
    - gcc
    - g++
    - build-base
    - binutils
    - make
    - libc-dev

entrypoint:
  command: /usr/bin/gcc

work-dir: /usr/src

paths:
  - path: /usr/src
    type: directory
    uid: 0
    gid: 0
    permissions: 0o755
