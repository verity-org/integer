include: ../../../_base/wolfi-dev.yaml

contents:
  packages:
    - erlang-28
    - erlang-dev
    - rebar3
    - build-base
    - git

entrypoint:
  command: /usr/bin/erl

work-dir: /app

paths:
  - path: /app
    type: directory
    uid: 0
    gid: 0
    permissions: 0o755
