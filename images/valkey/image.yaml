apiVersion: integer.verity.supply/v1alpha1
kind: ImageDefinition
name: valkey
description: "Valkey (Redis fork) — Wolfi rebuild eliminates persistent Debian CVEs"
upstream:
  package: valkey
versions:
  - version: "8"
    tags: ["8"]
    types: [default, fips]
  - version: "8.1"
    tags: ["8.1"]
    types: [default, fips]
  - version: "9.0"
    latest: true
    tags: ["9.0", "latest"]
    types: [default, fips]
