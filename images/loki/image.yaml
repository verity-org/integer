apiVersion: integer.verity.supply/v1alpha1
kind: ImageDefinition
name: loki
description: "Grafana Loki log aggregation — Wolfi rebuild eliminates Debian CVEs"
upstream:
  package: grafana-loki
versions:
  - version: "3"
    latest: true
    tags: ["3", "latest"]
    types: [default]
