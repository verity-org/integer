apiVersion: integer.verity.supply/v1alpha1
kind: ImageDefinition
name: influxdb
description: "InfluxDB time-series database — Wolfi rebuild eliminates Debian CVEs"
upstream:
  package: influxdb
versions:
  - version: "2"
    tags: ["2"]
    types: [default]
  - version: "3"
    latest: true
    tags: ["3", "latest"]
    types: [default]
