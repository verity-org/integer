include: ../../../_base/wolfi-base.yaml

contents:
  packages:
    - etcd

entrypoint:
  command: /usr/bin/etcd

paths:
  - path: /var/lib/etcd
    type: directory
    uid: 65532
    gid: 65532
    permissions: 0o700
