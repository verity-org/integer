apiVersion: integer.verity.supply/v1alpha1
kind: ImageDefinition
name: fluent-bit
description: "Fluent Bit log processor — Wolfi rebuild eliminates persistent CVEs in official image"
upstream:
  package: fluent-bit
versions:
  - version: "4"
    latest: true
    tags: ["4", "latest"]
    types: [default]
