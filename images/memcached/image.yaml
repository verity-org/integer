apiVersion: integer.verity.supply/v1alpha1
kind: ImageDefinition
name: memcached
description: "Memcached in-memory cache — Wolfi rebuild eliminates persistent Debian CVEs"
upstream:
  package: memcached
versions:
  - version: "1"
    latest: true
    tags: ["1", "latest"]
    types: [default]
