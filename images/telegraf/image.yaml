apiVersion: integer.verity.supply/v1alpha1
kind: ImageDefinition
name: telegraf
description: "Telegraf metrics agent — Wolfi rebuild eliminates Debian CVEs from official image"
upstream:
  package: telegraf
versions:
  - version: "1"
    latest: true
    tags: ["1", "latest"]
    types: [default]
