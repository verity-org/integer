apiVersion: integer.verity.supply/v1alpha1
kind: ImageDefinition
name: vault
description: "HashiCorp Vault secrets manager — Wolfi rebuild eliminates Debian CVEs"
upstream:
  package: vault
versions:
  - version: "1"
    latest: true
    tags: ["1", "latest"]
    types: [default, fips]
