apiVersion: integer.verity.supply/v1alpha1
kind: ImageDefinition
name: rabbitmq
description: "RabbitMQ message broker — Wolfi rebuild eliminates persistent Debian CVEs"
upstream:
  package: rabbitmq-server
versions:
  - version: "4"
    latest: true
    tags: ["4", "latest"]
    types: [default]
