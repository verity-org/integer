include: ../../../_base/wolfi-base.yaml

contents:
  packages:
    - ko
    - go-1.26

entrypoint:
  command: /usr/bin/ko

work-dir: /app

environment:
  GOPATH: /go
  KO_DEFAULTBASEIMAGE: cgr.dev/chainguard/static:latest
