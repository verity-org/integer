include: ../../../_base/wolfi-base.yaml

contents:
  packages:
    - redis

entrypoint:
  command: /usr/bin/redis-server

environment:
  REDIS_VERSION: "7"

paths:
  - path: /data
    type: directory
    uid: 65532
    gid: 65532
    permissions: 0o755
