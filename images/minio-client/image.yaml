apiVersion: integer.verity.supply/v1alpha1
kind: ImageDefinition
name: minio-client
description: "MinIO Client (mc) — Wolfi rebuild eliminates CVEs from official image"
upstream:
  package: minio-client
versions:
  - version: "RELEASE"
    latest: true
    tags: ["RELEASE", "latest"]
    types: [default]
