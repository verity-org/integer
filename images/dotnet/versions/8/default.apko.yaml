include: ../../../_base/wolfi-base.yaml

contents:
  packages:
    - dotnet-8-runtime

entrypoint:
  command: /usr/bin/dotnet

work-dir: /app

environment:
  DOTNET_RUNNING_IN_CONTAINER: "true"

paths:
  - path: /app
    type: directory
    uid: 65532
    gid: 65532
    permissions: 0o755
