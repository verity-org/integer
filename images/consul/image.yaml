apiVersion: integer.verity.supply/v1alpha1
kind: ImageDefinition
name: consul
description: "HashiCorp Consul service mesh — Wolfi rebuild eliminates Debian CVEs"
upstream:
  package: consul
versions:
  - version: "1"
    latest: true
    tags: ["1", "latest"]
    types: [default]
