include: ../../../_base/wolfi-base.yaml

contents:
  packages:
    - nodejs-20
    - libstdc++

entrypoint:
  command: /usr/bin/node

work-dir: /app

environment:
  NODE_ENV: production

paths:
  - path: /app
    type: directory
    uid: 65532
    gid: 65532
    permissions: 0o755
